name: nuevo_proyecto_final
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  file_picker: 
  flutter:
    sdk: flutter
  image_picker: ^0.8.4+4
  tflite: ^1.1.2

  cupertino_icons: ^1.0.2
  google_sign_in: ^6.1.0
  css_colors: ^1.1.1
  video_player: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
  assets:
    - lib/imagenes_historial/
